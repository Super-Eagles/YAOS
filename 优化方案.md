# YAOS 性能与资源占用优化方案

## 1. 现状及性能瓶颈分析

通过对 YAOS 项目源码的深度静态分析，已精准定位到导致系统在卡顿、CPU 满负荷（尤其是单个逻辑核心 100% 满载）以及磁盘 I/O 持续高额开销的底层根因。虽然业务逻辑相对清晰且不复杂，但由于以下四大核心机制的设计缺陷，在运行时产生了极其严重的**性能放大效应**。

### 1.1 无条件的 7 秒定时全量轮询
* **定位证据**：[`src/ui/StudioBridge.cpp:222`](file:///d:/GITHUB/YAOS/src/ui/StudioBridge.cpp#L222) 以及 [`src/ui/StudioBridge.cpp:891`](file:///d:/GITHUB/YAOS/src/ui/StudioBridge.cpp#L891)
* **设计瓶颈**：
  * 在 `StudioBridge` 初始化时，系统设置了 `m_refreshTimer.setInterval(7000)`，以固定的 7 秒间隔触发轮询，并在后台线程并发启动 `refreshCoreData()` 与 `refreshDeferredData()`。
  * 在每次刷新结束的收尾阶段，如果发现定时器没有启动，会无条件执行 `m_refreshTimer.start()`。
* **放大效应**：即使系统完全处于没有任何操作的闲置（Idle）状态，后台依然在以 7 秒为周期高频、无休止地运行极重级别的全量数据刷新，导致系统无法进入低功耗状态，持续消耗 CPU 资源。

### 1.2 全无内存缓存的 6 大 Store（全量文件加载与解析）
YAOS 拥有 6 个核心数据存储管理器，包括 `TaskStore`（任务）、`EventLog`（事件日志）、`ApprovalStore`（审批）、`NotificationCenter`（通知）、`AutomationStore`（自动化）以及 `AutomationRunStore`（自动化执行历史）。
* **定位证据**：
  * `TaskStore::loadUnlocked()` [`TaskStore.cpp:234-255`](file:///d:/GITHUB/YAOS/src/runtime/TaskStore.cpp#L234)
  * `EventLog::recentEvents()` [`EventLog.cpp:105-130`](file:///d:/GITHUB/YAOS/src/runtime/EventLog.cpp#L105)
  * `EventLog::count()` [`EventLog.cpp:132-146`](file:///d:/GITHUB/YAOS/src/runtime/EventLog.cpp#L132)
  * `ApprovalStore::count()` [`ApprovalStore.cpp:258-261`](file:///d:/GITHUB/YAOS/src/runtime/ApprovalStore.cpp#L258)
  * `NotificationCenter::count()` [`NotificationCenter.cpp:150`](file:///d:/GITHUB/YAOS/src/runtime/NotificationCenter.cpp#L150)
  * `AutomationStore::count()` [`AutomationStore.cpp:310`](file:///d:/GITHUB/YAOS/src/runtime/AutomationStore.cpp#L310)
* **设计瓶颈**：
  * **完全没有常驻内存缓存**。任何一次状态查询（如 `recentTasks(24)`、`recentEvents(80)`、甚至仅仅是 `count()` 获取总数）都会直接触发 `loadUnlocked()`，将整个对应的 JSON/JSONL 文件从磁盘重新读取出来、一次性全量解析为 JSON 树，并构建完整的数据结构。
  * `TaskStore` 在每次加载时还会额外运行高开销的递归 `hydrateTaskTree()` 树形计算，算法复杂度为 $O(N^2)$。
  * 甚至仅仅为了得到一个记录条数，`count()` 都会调用一次完整的 `loadUnlocked().size()`，导致整盘数据重新读取并反序列化一遍。

### 1.3 `ResourceCatalog::collectResources()` 的超强性能放大效应
* **定位证据**：[`src/runtime/ResourceCatalog.cpp:56-197`](file:///d:/GITHUB/YAOS/src/runtime/ResourceCatalog.cpp#L56) 和 [`ResourceCatalog::summary()`](file:///d:/GITHUB/YAOS/src/runtime/ResourceCatalog.cpp#L199)
* **设计瓶颈**：
  * 在 7 秒轮询中，后台线程会触发 `recentResources(120)`，它会调用 `collectResources()`。
  * `collectResources()` 会在一瞬间执行以下所有极度耗时的任务：
    1. 遍历 `sessions/*.jsonl` 目录下的所有对话会话。
    2. 打开并**逐行读取每个会话文件**的行数，以计算消息条数。
    3. 在栈上构造新的 `TaskStore` 并调用 `recentTasks(200)` -> 触发 `tasks.json` 的全盘读取与解析。
    4. 构造 `EventLog` 并调用 `recentEvents(200)` -> 触发 `events.jsonl` 的全盘逐行读取与解析（文件最大可达 4MB）。
    5. 构造 `ApprovalStore` 并调用 `recentApprovals(200)` -> 触发全盘文件解析。
    6. 构造 `NotificationCenter` 并调用 `recentNotifications(200)` -> 触发全盘文件解析。
    7. 构造 `AutomationStore` 并调用 `list(200)` -> 触发全盘文件解析。
    8. 构造 `PluginRegistry` 并扫描插件目录、对每个插件的 `plugin.json` 执行文件读取。
    9. 构造 `SkillRegistry` 执行类似的物理磁盘扫描。
    10. 将以上所有海量数据收集进一个 QVector，并对其进行排序。
  * 更为雪上加霜的是，在 coreData 轮询中还会单独调用 `m_backend->resourceSummary()`，再次瞬间执行 `ResourceCatalog::summary()`。这会导致上述所有的 Store `count()` 函数又齐刷刷重新把所有磁盘文件物理加载、反序列化了一遍！

### 1.4 AI 运行期间 `chatProgressTimer` 的毫秒级高频读取
* **定位证据**：[`src/ui/StudioBridge.cpp:239`](file:///d:/GITHUB/YAOS/src/ui/StudioBridge.cpp#L239) 与 [`StudioBridge::updateChatProgress()`](file:///d:/GITHUB/YAOS/src/ui/StudioBridge.cpp#L2149)（在此调用 `backend->recentEvents(160)`）
* **设计瓶颈**：
  * 当 AI 正在处理任务时，前台界面的 `chatProgressTimer` 会以每 **240毫秒** 一次的极高频率不断触发更新。
  * 每次触发都会强制调用 `recentEvents(160)`，意味着在 AI 生成回复的过程中，每秒大约有 4 次要把几兆大小的 `events.jsonl` 文件从磁盘读取并解析一次。这导致 CPU 瞬间拉满，并且前台界面甚至开始明显的粘滞、卡顿。

---

## 2. 优化方案设计（低侵入性、高性能）

为了确保不改变项目核心逻辑、零编译、零破环，并且尽可能以**最小的修改代价**获取**最大的性能提升**，我们制定了以下 4 项优化方案，并按优先级从高到低排列：

### 2.1 【P1 优先级】为核心 Store 增加内存缓存层 (Cache Layer)
* **核心思想**：彻底消灭“读放大”现象。只在数据初始化、或文件实际发生改变时进行磁盘读取，平时数据直接从内存返回。
* **具体做法**：
  1. 在 `TaskStore`、`EventLog`、`ApprovalStore`、`NotificationCenter`、`AutomationStore` 等 Store 类中声明 `bool m_cacheLoaded = false;` 和 `QVector<RecordType> m_cachedRecords;`，并加入 `mutable QMutex m_cacheMutex;` 进行线程安全保护。
  2. 重构 `loadUnlocked()`：
     ```cpp
     QVector<RecordType> Store::loadUnlocked() const {
         QMutexLocker locker(&m_cacheMutex);
         if (m_cacheLoaded) {
             return m_cachedRecords; // 直接返回内存缓存，时间复杂度从 O(N) 变为 O(1)
         }
         // 仅在首次或失效时从磁盘加载
         m_cachedRecords = doActualDiskLoad();
         m_cacheLoaded = true;
         return m_cachedRecords;
     }
     ```
  3. 重构所有修改数据的写方法（如 `append`、`saveUnlocked` 等）：在成功写入磁盘后，同步更新 `m_cachedRecords` 或将 `m_cacheLoaded` 设为 `false`，确保缓存的一致性。
  4. 重构 `count()` 方法：
     ```cpp
     int Store::count() const {
         return loadUnlocked().size(); // 此时 loadUnlocked() 直接走内存缓存，性能飞跃
     }
     ```
* **预期效果**：99.9% 消除 Store 类带来的重复磁盘 I/O 与高额解析 CPU 损耗，轮询操作耗时缩短 95% 以上。

### 2.2 【P2 优先级】将 7秒定时器轮询 改为 事件驱动刷新机制
* **核心思想**：告别定时器死轮询，做到“无事不刷，有事即刷”。
* **具体做法**：
  1. 使用 Qt 的信号与槽（Signal/Slot）机制。当任何 Store 执行了数据的保存/添加/更新操作时，主动发射对应的更新信号（例如 `emit tasksChanged()`）。
  2. 在 `StudioBridge` 中连接这些信号到 `refreshCoreData()` 和 `refreshDeferredData()`。
  3. 对于外部文件变化（如用户手动编辑 JSON 文件），在 `StudioBridge` 内部实例化 `QFileSystemWatcher`，监听 `runtime/` 和 `sessions/` 目录：
     ```cpp
     m_fileWatcher.addPath(workspacePath + "/runtime/tasks.json");
     connect(&m_fileWatcher, &QFileSystemWatcher::fileChanged, this, &StudioBridge::refreshAll);
     ```
  4. 彻底废除无条件的 7秒定时器：只有在极端兜底的情况下，才保留一个例如 60 秒的超低频定时器，或者完全去除该定时器。
* **预期效果**：在用户闲置时，`yaos.exe` 的 CPU 占用率接近 **0%**；仅在发生实质性状态改变或文件编辑时才触发微秒级的数据刷新。

### 2.3 【P3 优先级】优化 `ResourceCatalog` 搜集机制
* **核心思想**：避免“满汉全席”式的全量搜索，实施“懒加载”与“增量更新”。
* **具体做法**：
  1. **对话消息计数懒加载**：`countSessionMessages` 不应该在每次 `collectResources()` 时对 sessions 目录下的每一个 `.jsonl` 都做全量文件打开并读行数。可以通过在内存中维护一个 `QHash<QString, QPair<QDateTime, int>> m_sessionLineCountCache;`（记录文件名 -> 对应文件的【最后修改时间 + 行数】）。只有在文件修改时间变新时，才去重新读取并计算，否则直接返回缓存条数。
  2. **避免重复创建 Store**：不要在 `collectResources()` 中在栈上频繁临时创建 Store 对象，而是保持 Store 对象的单例或在 `ResourceCatalog` 中作为长生命周期的成员变量，配合 P1 的缓存层，瞬间返回结果。
* **预期效果**：`recentResources` 收集操作从原先的数百毫秒直接降至 1 毫秒以内。

### 2.4 【P4 优先级】增量读取 EventLog & 降频 chatProgressTimer
* **核心思想**：解决 AI 运行时的 CPU 脉冲满载和界面卡顿问题。
* **具体做法**：
  1. **chatProgressTimer 降频**：将 `m_chatProgressTimer.setInterval(240)` 调整为更合理的 `500` 毫秒（半秒触发一次对人眼而言依旧极其流畅，且直接将频次和负荷降低了一半）。
  2. **增量加载**：在 AI 运行时，前端只需要获取最新的几行日志。`EventLog::recentEvents` 可以引入高效的反向读取机制（自底向上读取文件最后 N 行，而不是把整个几兆的文件从头到尾读一遍），或使用 P1 内存缓存同步向内存中 append 最新的日志行，避免任何解析历史旧数据的开销。
* **预期效果**：AI 运行期间，界面的卡顿和延迟感完全消失，CPU 占用率从 100% 暴跌至极低。

---

## 3. 改动代码量与技术风险估算

| 优化模块 | 涉及关键文件 | 预估修改行数 | 技术风险等级 | 备注说明 |
| :--- | :--- | :--- | :--- | :--- |
| **P1: Store 缓存** | `TaskStore.h/cpp`<br>`EventLog.h/cpp`<br>`ApprovalStore.h/cpp`<br>`NotificationCenter.h/cpp`<br>`AutomationStore.h/cpp` | ~150 行 | **极低** | 仅属于内部数据获取的性能优化，完全不影响外部接口的调用方式和逻辑 |
| **P2: 事件驱动轮询** | `StudioBridge.h/cpp` | ~80 行 | **低** | 需要解除原本 7 秒 Timer 的死绑定，注意信号槽并发触发时的去抖（Debounce） |
| **P3: Session计数缓存**| `ResourceCatalog.h/cpp` | ~40 行 | **极低** | 属于典型的局部计算缓存，风险基本为零 |
| **P4: 进度更新优化** | `StudioBridge.cpp`<br>`EventLog.cpp` | ~30 行 | **极低** | 修改定时器间隔为安全操作；EventLog 的增量返回可通过缓存极速实现 |

---

## 4. 优化效果验证方法

优化方案实施后，可通过以下三种方式进行闭环验证，确保系统性能确实得到实质性改善：

### 4.1 资源监控与性能对比指标
1. **CPU 闲置率对比**：
   * 优化前：YAOS 处于闲置无任务状态时，`yaos.exe` 的 CPU 会有规律地每 7 秒出现一次约 10%~15% 的尖峰脉冲，总 CPU 保持中高负荷。
   * 优化后：闲置状态下，`yaos.exe` CPU 曲线应当绝对平稳，接近 **0.0%**。
2. **I/O 读写对比（通过资源监视器）**：
   * 优化前：每 7 秒在 `runtime/` 和 `sessions/` 目录下产生密集的读磁盘 I/O。
   * 优化后：在闲置状态下，对上述目录的磁盘读写次数降为 **0 次**。
3. **AI 对话运行期间的系统平滑度**：
   * 优化前：AI 回复生成期间，CPU 满载至 100%，鼠标移动和输入法打字产生明显粘滞延迟。
   * 优化后：AI 回复期间界面依然行云流水，输入流畅无阻。

### 4.2 验证用例与日志诊断
* 可通过在 [`src/ui/StudioBridge.cpp`](file:///d:/GITHUB/YAOS/src/ui/StudioBridge.cpp) 中的 `refreshCoreData()` 和 `refreshDeferredData()` 入口处保留微秒级计时器（`QElapsedTimer`），对比单次刷新操作的耗时变化：
  * **优化前**：单次 Core+Deferred 刷新耗时约 50 ~ 200 ms。
  * **优化后**：耗时应当降到 **1 ms 以下**。
